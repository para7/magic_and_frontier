package views


templ GrimoirePage(data GrimoirePageData) {
	@Layout(data.Meta.Title, data.Meta.CurrentPath) {
		@PageScaffold(data.Meta, data.Notice) {
			@GrimoireShell(data)
		}
	}
}

templ GrimoireShell(data GrimoirePageData) {
	<div id="feature-shell" class="feature-shell">
		@NoticeBox(data.Notice)
		<div class="feature-grid">
			<section class="form-span-2">
				@ListBrowser(data.Meta.CurrentURL, "/grimoire/new", "新規追加", "id, name, cast 情報で検索") {
					<div class="entry-list" data-list-entries>
						for _, entry := range data.Entries {
							<article
								class="entry-card"
								data-list-item
								data-search={ SearchBlob(entry.ID, entry.Title, entry.Description, IntText(entry.CastID), IntText(entry.CastTime), IntText(entry.MPCost), entry.UpdatedAt) }
								data-filters="all"
								data-sort-updated={ entry.UpdatedAt }
								data-sort-id={ entry.ID }
								data-sort-title={ entry.Title }
								data-sort-cast_id={ IntText(entry.CastID) }>
								<header>
									<div>
										<h3 class="entry-line code">{ BracketText(entry.ID) + " " + DisplayText(entry.Title, "-") }</h3>
										<p class="entry-meta entry-line code">{ JoinCompactWithComma(IntText(entry.CastTime), IntText(entry.MPCost)) }</p>
									</div>
									<div class="entry-actions">
										<a class="secondary outline" role="button" href={ WithReturnTo("/grimoire/edit?id="+entry.ID, data.Meta.CurrentURL) } data-return-to-link={ "/grimoire/edit?id=" + entry.ID }>編集</a>
										<form method="post" action={ "/grimoire/" + entry.ID + "/delete" }>
											@ReturnToField(data.Meta.CurrentURL)
											<button type="submit" class="danger">削除</button>
										</form>
									</div>
								</header>
							</article>
						}
						@ListEmptyState("まだ魔導書エントリはありません。", len(data.Entries) > 0)
					</div>
				}
			</section>
		</div>
	</div>
}

templ GrimoireFormPage(data GrimoirePageData) {
	@Layout(data.Meta.Title, data.Meta.CurrentPath) {
		@PageScaffold(data.Meta, data.Notice) {
			@GrimoireFormShell(data)
		}
	}
}

templ GrimoireFormShell(data GrimoirePageData) {
	<div id="feature-shell" class="feature-shell">
		@NoticeBox(data.Notice)
		<div class="feature-grid">
			<section class="form-span-2">
				@FormShellHeader(FormTitle("Grimoire Entry", data.Form.IsEditing))
				<form method="post" action={ FormAction("/grimoire", data.Form.IsEditing) }>
					@ReturnToField(data.Form.ReturnTo)
					<div class="form-blocks">
						<div class="form-block stack">
							<label>
								ID
								if data.Form.IsEditing {
									<input name="id" value={ data.Form.ID } readonly/>
								} else {
									<input name="id" value={ data.Form.ID }/>
									<small>任意のIDを入力（空白のみ不可）</small>
								}
								@FieldErrorBox(data.Form.FieldErrors, "id")
							</label>
							<label>
								Cast ID
								if data.Form.IsEditing {
									<input class="input-short" name="castid" value={ data.Form.CastID } readonly/>
								} else {
									<input class="input-short" name="castid" value={ data.Form.CastID }/>
									<small>1以上の整数</small>
								}
								@FieldErrorBox(data.Form.FieldErrors, "castid")
							</label>
							<label>
								Cast Time
								<input class="input-short" name="castTime" value={ data.Form.CastTime }/>
								@FieldErrorBox(data.Form.FieldErrors, "castTime")
							</label>
							<label>
								MP Cost
								<input class="input-short" name="mpCost" value={ data.Form.MPCost }/>
								@FieldErrorBox(data.Form.FieldErrors, "mpCost")
							</label>
							<label>
								Title
								<input class="input-medium" name="title" value={ data.Form.Title }/>
								@FieldErrorBox(data.Form.FieldErrors, "title")
							</label>
						</div>
						<div class="form-block stack">
							<label class="wide">
								Description
								<textarea name="description">{ data.Form.Description }</textarea>
							</label>
							<label class="wide">
								Script
								<textarea name="script" rows="10">{ data.Form.Script }</textarea>
								@FieldErrorBox(data.Form.FieldErrors, "script")
							</label>
						</div>
					</div>
					if data.Form.FormError != "" {
						<div class="field-error">{ data.Form.FormError }</div>
					}
					<div class="form-actions">
							<button type="submit">{ SubmitLabel(data.Form.IsEditing) }</button>
							<a class="secondary outline" role="button" href={ BackLink("/grimoire", data.Form.ReturnTo) }>キャンセル</a>
					</div>
				</form>
			</section>
		</div>
	</div>
}
